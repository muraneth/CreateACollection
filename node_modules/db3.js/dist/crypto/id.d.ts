export declare class TxId {
    data: Uint8Array;
    constructor(data: Uint8Array);
    static from(hash: Uint8Array): TxId;
    getB64(): string;
}
export declare class DbId {
    addr: string;
    constructor(sender: string, nonce: number);
    getHexAddr(): string;
}
