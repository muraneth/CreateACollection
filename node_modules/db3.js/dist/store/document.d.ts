import { CollectionReference } from './collection';
import { Query, QueryResult } from './query';
import { DocumentEntry, DocumentData } from '../client/client';
export declare class DocumentReference<T = DocumentData> {
    /** The type of this Firestore reference. */
    readonly type = "document";
    /**
     * The {@link Firestore} instance the document is in.
     * This is useful for performing transactions, for example.
     */
    readonly collection: CollectionReference<T>;
    readonly entry: DocumentEntry<T>;
    /** @hideconstructor */
    constructor(collection: CollectionReference<T>, entry: DocumentEntry<T>);
}
export declare function addDoc<T = DocumentData>(reference: CollectionReference<T>, data: T): Promise<any>;
export declare function getDocs<T = DocumentData>(query: Query): Promise<QueryResult<T>>;
export declare function deleteDoc<T = DocumentData>(reference: DocumentReference<T>): Promise<void>;
export declare function updateDoc<T = DocumentData>(reference: DocumentReference<T>, data: any): Promise<void>;
