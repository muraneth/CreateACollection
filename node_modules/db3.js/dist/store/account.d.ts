import { DB3Client } from '../client/client';
export declare class DB3Account {
    readonly address: string;
    readonly client: DB3Client;
    constructor(address: string, client: DB3Client);
    getAccount(): Promise<void>;
}
export declare function bytesToReadableNum(bytes_size: number): string;
export declare function unitsToReadableNum(units: number): string;
